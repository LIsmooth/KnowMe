<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.leif.knowme.mapper.TodoMapper">

    <insert id="createTodo" parameterType="TodoDo">
        insert into t_todo(todo_id, user_id, plan_consumed, real_consumed, event_msg, status)
        values (#{todoId},#{userId}, #{planConsumed}, #{realConsumed}, #{eventMsg},#{status})
    </insert>


    <delete id="deleteAllByUserId" parameterType="String">
        delete from t_todo where user_id=#{userId}
    </delete>
    <delete id="deleteByTodoId">
        delete
        from t_todo
        where todo_id = #{todoId}
    </delete>

    <select id="getUserAllTodos" resultType="TodoDo">
        select *
        from t_todo
        where user_id = #{0}
    </select>

    <select id="generateSchedule" resultType="TodoDo">
        select * from t_todo where user_id=#{userId} order by created;
    </select>


    <update id="updateTodo" parameterType="TodoDo">
        update t_todo
        set event_msg=#{event_msg},
            plan_consumed = #{planConsumed}
        where todo_id = #{todoId}
    </update>


</mapper>