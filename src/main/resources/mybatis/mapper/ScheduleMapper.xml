<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.leif.knowme.mapper.ScheduleMapper">

    <insert id="createSchedule">
        insert into t_schedule (schedule_id, account_id, schedule_name, schedule_note, plan_start_time, plan_end_time)
            VALUE (#{scheduleId}, #{userId}, #{scheduleName}, #{scheduleNote}, #{planStartTime}, #{planEndTime})
    </insert>

    <insert id="saveScheduleItems" useGeneratedKeys="true" keyProperty="item_id">
        insert into t_schedule_item (schedule_id, todo_id, plan_start_time, plan_end_time, order_no) values
        <foreach item="item" collection="list" separator=",">
            (#{item.schedule_id}, #{item.todo_id}, #{item.plan_start_time}, #{item.plan_end_time},#{item.order_no})
        </foreach>
    </insert>
</mapper>